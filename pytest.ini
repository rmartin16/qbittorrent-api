[pytest]
testpaths = tests
norecursedirs = dist build .tox scripts
filterwarnings =
  error
addopts =
  --doctest-modules
  --cov=qbittorrentapi
  --cov-report=xml
  --cov-report=html
  --no-cov-on-fail
  -r a
  -v
